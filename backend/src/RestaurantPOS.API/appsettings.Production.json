{
  "//": "Packaged desktop install. The database path, JWT key path and attachments path are supplied as environment variables by the desktop shell (see frontend/electron/main.ts) so they land in a writable per-machine folder rather than under Program Files.",
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  }
}
