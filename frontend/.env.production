# Packaged desktop build. The API is spawned by the Electron shell on this same machine and
# listens on port 5207 (see frontend/electron/main.ts and ASPNETCORE_URLS there).
VITE_API_URL=http://localhost:5207/api/v1
VITE_WS_URL=http://localhost:5207
VITE_API_MOCKING=false
